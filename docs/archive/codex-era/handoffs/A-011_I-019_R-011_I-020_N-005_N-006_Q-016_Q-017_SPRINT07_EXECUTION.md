# Task
- ID: A-011, I-019, R-011, I-020, N-005, N-006, Q-016, Q-017
- Title: Sprint 07 full execution (workflow + scene IO + mesh import + bake/export + validation)
- Role: ArchitectAgent, InteractionAgent, RuntimeAgent, AnimationAgent, QAAgent

# Summary
- What changed:
  - Updated contracts for Sprint 07:
    - workflow ownership and tool gating (`Rigging`, `Animation`, `Puppeteering`)
    - versioned scene envelope and migration/failure policy
    - bake cache and export job capability matrix
  - Added runtime/animation services:
    - `src/runtime/workflow.ts`
    - `src/runtime/scenePersistence.ts`
    - `src/runtime/meshImport.ts`
    - `src/animation/bakeCache.ts`
    - `src/animation/exportPipeline.ts` (BVH implemented, FBX/GLB explicit unsupported)
  - Integrated App scaffolding:
    - workflow switcher UI and deterministic transition handling
    - mode-aware action/tool gating
    - scene save/load controls
    - mesh import controls (file picker + drag/drop)
    - bake capture and export controls with status surfaces
  - Added automated tests for all Sprint 07 seams:
    - `workflow-modes`, `scene-persistence`, `mesh-import`, `bake-export`
  - Updated sprint planning and manual validation checklist with Sprint 07 evidence.
- Why:
  - Deliver Sprint 07 scoped scaffolding with explicit deterministic behavior, explicit unsupported paths, and continuity-ready documentation.

# Contract References
- Files:
  - `docs/contracts/MODE_TOOL_STATE.md`
  - `docs/contracts/SCENE_SCHEMA.md`
  - `docs/contracts/RIG_RUNTIME.md`
  - `docs/contracts/TIMELINE_RECORDING.md`
- Contract change required: yes

# Validation
- Unit/integration/manual evidence:
  - `npm test` passed.
  - `npm run build` passed.
  - Added Sprint 07 manual checklist evidence in `docs/checklists/XR_DESKTOP_VALIDATION.md`.
- Perf notes:
  - No new heavy runtime loops beyond optional bake/export actions.
  - Existing Vite chunk-size warnings remain (pre-existing warning class).

# Risks / Follow-ups
- Known issues:
  - Mesh import baseline path is `FBX`; other formats are explicit unsupported scaffolds.
  - Scene envelope currently serializes through existing `SceneDocument` (`0.1.0`) payload shape inside a `1.0.0` transport envelope.
  - BVH export is baseline scaffolding; advanced hierarchy semantics (multiple disconnected roots and richer channels) remain future work.
- Next owner:
  - QAAgent + ArchitectAgent for release gate sign-off and any Sprint 08 scope decomposition.
